@{
    // View
    View = this;

    // Check action result
    if (crewBankAccountForAdminEdit.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!crewBankAccountForAdminEdit.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@crewBankAccountForAdminEdit.ShowPageHeader()
@crewBankAccountForAdminEdit.ShowMessages()
<main class="edit">
<form name="fCrewBankAccountForAdminedit" id="fCrewBankAccountForAdminedit" class="@(crewBankAccountForAdminEdit.FormClassName)" action="@AppPath(CurrentPageName())" method="post" autocomplete="on">
<script>
var currentTable = @Html.Raw(ConvertToJson(crewBankAccountForAdminEdit.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { CrewBankAccountForAdmin: currentTable } });
var currentPageID = ew.PAGE_ID = "edit";
var currentForm;
var fCrewBankAccountForAdminedit;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fCrewBankAccountForAdminedit")
        .setPageId("edit")

        // Add fields
        .setFields([

            ["AccountNumber", [fields.AccountNumber.visible && fields.AccountNumber.required ? ew.Validators.required(fields.AccountNumber.caption) : null], fields.AccountNumber.isInvalid],
            ["Beneficiary", [fields.Beneficiary.visible && fields.Beneficiary.required ? ew.Validators.required(fields.Beneficiary.caption) : null], fields.Beneficiary.isInvalid],
            ["MTCurrencyID", [fields.MTCurrencyID.visible && fields.MTCurrencyID.required ? ew.Validators.required(fields.MTCurrencyID.caption) : null], fields.MTCurrencyID.isInvalid],
            ["MainAcc", [fields.MainAcc.visible && fields.MainAcc.required ? ew.Validators.required(fields.MainAcc.caption) : null], fields.MainAcc.isInvalid],
            ["Attachment", [fields.Attachment.visible && fields.Attachment.required ? ew.Validators.fileRequired(fields.Attachment.caption) : null], fields.Attachment.isInvalid],
            ["MTCrewID", [fields.MTCrewID.visible && fields.MTCrewID.required ? ew.Validators.required(fields.MTCrewID.caption) : null], fields.MTCrewID.isInvalid],
            ["MTBankID", [fields.MTBankID.visible && fields.MTBankID.required ? ew.Validators.required(fields.MTBankID.caption) : null], fields.MTBankID.isInvalid],
            ["OtherBankName", [fields.OtherBankName.visible && fields.OtherBankName.required ? ew.Validators.required(fields.OtherBankName.caption) : null], fields.OtherBankName.isInvalid]
        ])

        // Form_CustomValidate
        .setCustomValidate(
            function(fobj) { // DO NOT CHANGE THIS LINE!
                    // Your custom validation code here, return false if invalid.
                    return true;
                }
        )

        // Use JavaScript validation or not
        .setValidateRequired(ew.CLIENT_VALIDATE)

        // Dynamic selection lists
        .setLists({
            "MTCurrencyID": @Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.ToClientList(crewBankAccountForAdminEdit)),
            "MainAcc": @Html.Raw(crewBankAccountForAdminEdit.MainAcc.ToClientList(crewBankAccountForAdminEdit)),
            "MTBankID": @Html.Raw(crewBankAccountForAdminEdit.MTBankID.ToClientList(crewBankAccountForAdminEdit)),
        })
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
@if (CurrentPage.CheckToken) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="update">
<input type="hidden" name="modal" value="@ConvertToInt(crewBankAccountForAdminEdit.IsModal)">
@if (IsJsonResponse()) {
<input type="hidden" name="json" value="1">
}
<input type="hidden" name="@(crewBankAccountForAdminEdit.OldKeyName)" value="@Html.Raw(crewBankAccountForAdminEdit.OldKey)">
<div class="ew-edit-div d-none"><!-- page* -->
@if (crewBankAccountForAdminEdit.AccountNumber.Visible) { // crewBankAccountForAdminEdit.AccountNumber.Visible (begin)
    <div id="r_AccountNumber" @Html.Raw(crewBankAccountForAdminEdit.AccountNumber.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_AccountNumber" for="x_AccountNumber" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_AccountNumber">@Html.Raw(crewBankAccountForAdminEdit.AccountNumber.Caption)@Html.Raw(crewBankAccountForAdminEdit.AccountNumber.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.AccountNumber.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_AccountNumber"><span id="el_CrewBankAccountForAdmin_AccountNumber">
<input type="@(crewBankAccountForAdminEdit.AccountNumber.InputTextType)" name="x_AccountNumber" id="x_AccountNumber" data-table="CrewBankAccountForAdmin" data-field="x_AccountNumber" value="@Html.Raw(crewBankAccountForAdminEdit.AccountNumber.EditValue)" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(crewBankAccountForAdminEdit.AccountNumber.PlaceHolder))" data-format-pattern="@(crewBankAccountForAdminEdit.AccountNumber.FormatPattern)"@Html.Raw(crewBankAccountForAdminEdit.AccountNumber.EditAttributes) aria-describedby="x_AccountNumber_help">
@Html.Raw(crewBankAccountForAdminEdit.AccountNumber.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.AccountNumber.GetErrorMessage())</div>
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.AccountNumber.Visible (end) *@
@if (crewBankAccountForAdminEdit.Beneficiary.Visible) { // crewBankAccountForAdminEdit.Beneficiary.Visible (begin)
    <div id="r_Beneficiary" @Html.Raw(crewBankAccountForAdminEdit.Beneficiary.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_Beneficiary" for="x_Beneficiary" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_Beneficiary">@Html.Raw(crewBankAccountForAdminEdit.Beneficiary.Caption)@Html.Raw(crewBankAccountForAdminEdit.Beneficiary.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.Beneficiary.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_Beneficiary"><span id="el_CrewBankAccountForAdmin_Beneficiary">
<input type="@(crewBankAccountForAdminEdit.Beneficiary.InputTextType)" name="x_Beneficiary" id="x_Beneficiary" data-table="CrewBankAccountForAdmin" data-field="x_Beneficiary" value="@Html.Raw(crewBankAccountForAdminEdit.Beneficiary.EditValue)" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(crewBankAccountForAdminEdit.Beneficiary.PlaceHolder))" data-format-pattern="@(crewBankAccountForAdminEdit.Beneficiary.FormatPattern)"@Html.Raw(crewBankAccountForAdminEdit.Beneficiary.EditAttributes) aria-describedby="x_Beneficiary_help">
@Html.Raw(crewBankAccountForAdminEdit.Beneficiary.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.Beneficiary.GetErrorMessage())</div>
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.Beneficiary.Visible (end) *@
@if (crewBankAccountForAdminEdit.MTCurrencyID.Visible) { // crewBankAccountForAdminEdit.MTCurrencyID.Visible (begin)
    <div id="r_MTCurrencyID" @Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_MTCurrencyID" for="x_MTCurrencyID" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_MTCurrencyID">@Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.Caption)@Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_MTCurrencyID"><span id="el_CrewBankAccountForAdmin_MTCurrencyID">
    <select
        id="x_MTCurrencyID"
        name="x_MTCurrencyID"
        class="form-select ew-select@(crewBankAccountForAdminEdit.MTCurrencyID.IsInvalidClass)"
        data-select2-id="fCrewBankAccountForAdminedit_x_MTCurrencyID"
        data-table="CrewBankAccountForAdmin"
        data-field="x_MTCurrencyID"
        data-value-separator="@(crewBankAccountForAdminEdit.MTCurrencyID.DisplayValueSeparatorAttribute)"
        data-placeholder="@(HtmlEncode(crewBankAccountForAdminEdit.MTCurrencyID.PlaceHolder))"
        @Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.EditAttributes)>
        @(crewBankAccountForAdminEdit.MTCurrencyID.SelectOptionListHtml("x_MTCurrencyID"))
    </select>
    @Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.GetCustomMessage)
    <div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.GetErrorMessage())</div>
@Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.Lookup.GetParamTag(crewBankAccountForAdminEdit, "p_x_MTCurrencyID"))
<script>
loadjs.ready("fCrewBankAccountForAdminedit", function() {
    var options = { name: "x_MTCurrencyID", selectId: "fCrewBankAccountForAdminedit_x_MTCurrencyID" },
        el = document.querySelector("select[data-select2-id='" + options.selectId + "']");
    if (!el)
        return;
    options.closeOnSelect = !options.multiple;
    options.dropdownParent = el.closest("#ew-modal-dialog, #ew-add-opt-dialog");
    if (fCrewBankAccountForAdminedit.lists.MTCurrencyID?.lookupOptions.length) {
        options.data = { id: "x_MTCurrencyID", form: "fCrewBankAccountForAdminedit" };
    } else {
        options.ajax = { id: "x_MTCurrencyID", form: "fCrewBankAccountForAdminedit", limit: ew.LOOKUP_PAGE_SIZE };
    }
    options.minimumInputLength = ew.selectMinimumInputLength;
    options = Object.assign({}, ew.selectOptions, options, ew.vars.tables.CrewBankAccountForAdmin.fields.MTCurrencyID.selectOptions);
    ew.createSelect(options);
});
</script>
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.MTCurrencyID.Visible (end) *@
@if (crewBankAccountForAdminEdit.MainAcc.Visible) { // crewBankAccountForAdminEdit.MainAcc.Visible (begin)
    <div id="r_MainAcc" @Html.Raw(crewBankAccountForAdminEdit.MainAcc.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_MainAcc" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_MainAcc">@Html.Raw(crewBankAccountForAdminEdit.MainAcc.Caption)@Html.Raw(crewBankAccountForAdminEdit.MainAcc.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.MainAcc.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_MainAcc"><span id="el_CrewBankAccountForAdmin_MainAcc">
<div class="form-check d-inline-block">
    <input type="checkbox" class="form-check-input@(crewBankAccountForAdminEdit.MainAcc.IsInvalidClass)" data-table="CrewBankAccountForAdmin" data-field="x_MainAcc" data-boolean name="x_MainAcc" id="x_MainAcc" value="1" checked="@ConvertToBool(crewBankAccountForAdminEdit.MainAcc.CurrentValue)" @Html.Raw(crewBankAccountForAdminEdit.MainAcc.EditAttributes) aria-describedby="x_MainAcc_help">
    <div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.MainAcc.GetErrorMessage())</div>
</div>
@Html.Raw(crewBankAccountForAdminEdit.MainAcc.GetCustomMessage)
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.MainAcc.Visible (end) *@
@if (crewBankAccountForAdminEdit.Attachment.Visible) { // crewBankAccountForAdminEdit.Attachment.Visible (begin)
    <div id="r_Attachment" @Html.Raw(crewBankAccountForAdminEdit.Attachment.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_Attachment" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_Attachment">@Html.Raw(crewBankAccountForAdminEdit.Attachment.Caption)@Html.Raw(crewBankAccountForAdminEdit.Attachment.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.Attachment.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_Attachment"><span id="el_CrewBankAccountForAdmin_Attachment">
<div id="fd_x_Attachment" class="fileinput-button ew-file-drop-zone">
    <input
        type="file"
        id="x_Attachment"
        name="x_Attachment"
        class="form-control ew-file-input"
        title="@(crewBankAccountForAdminEdit.Attachment.Title)"
        lang="@(CurrentLanguageID)"
        data-table="CrewBankAccountForAdmin"
        data-field="x_Attachment"
        data-size="150"
        data-accept-file-types="@(crewBankAccountForAdminEdit.Attachment.AcceptFileTypes)"
        data-max-file-size="@(crewBankAccountForAdminEdit.Attachment.UploadMaxFileSize)"
        data-max-number-of-files="null"
        data-disable-image-crop="@(crewBankAccountForAdminEdit.Attachment.ImageCropper ? 0 : 1)"
        aria-describedby="x_Attachment_help"
        @Html.Raw((crewBankAccountForAdminEdit.Attachment.ReadOnly || crewBankAccountForAdminEdit.Attachment.Disabled) ? " disabled" : "")
        @Html.Raw(crewBankAccountForAdminEdit.Attachment.EditAttributes)
    >
    <div class="text-muted ew-file-text">@Html.Raw(Language.Phrase("ChooseFile"))</div>
</div>
@Html.Raw(crewBankAccountForAdminEdit.Attachment.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.Attachment.GetErrorMessage())</div>
<input type="hidden" name="fn_x_Attachment" id= "fn_x_Attachment" value="@(crewBankAccountForAdminEdit.Attachment.Upload.FileName)">
<input type="hidden" name="fa_x_Attachment" id= "fa_x_Attachment" value="@(Post("fa_x_Attachment") == "0" ? "0" : "1")">
<table id="ft_x_Attachment" class="table table-sm float-start ew-upload-table"><tbody class="files"></tbody></table>
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.Attachment.Visible (end) *@
@if (crewBankAccountForAdminEdit.MTCrewID.Visible) { // crewBankAccountForAdminEdit.MTCrewID.Visible (begin)
    <div id="r_MTCrewID" @Html.Raw(crewBankAccountForAdminEdit.MTCrewID.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_MTCrewID" for="x_MTCrewID" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_MTCrewID">@Html.Raw(crewBankAccountForAdminEdit.MTCrewID.Caption)@Html.Raw(crewBankAccountForAdminEdit.MTCrewID.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.MTCrewID.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_MTCrewID"><span id="el_CrewBankAccountForAdmin_MTCrewID">
<input type="@(crewBankAccountForAdminEdit.MTCrewID.InputTextType)" name="x_MTCrewID" id="x_MTCrewID" data-table="CrewBankAccountForAdmin" data-field="x_MTCrewID" value="@Html.Raw(crewBankAccountForAdminEdit.MTCrewID.EditValue)" size="30" placeholder="@Html.Raw(HtmlEncode(crewBankAccountForAdminEdit.MTCrewID.PlaceHolder))" data-format-pattern="@(crewBankAccountForAdminEdit.MTCrewID.FormatPattern)"@Html.Raw(crewBankAccountForAdminEdit.MTCrewID.EditAttributes) aria-describedby="x_MTCrewID_help">
@Html.Raw(crewBankAccountForAdminEdit.MTCrewID.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.MTCrewID.GetErrorMessage())</div>
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.MTCrewID.Visible (end) *@
@if (crewBankAccountForAdminEdit.MTBankID.Visible) { // crewBankAccountForAdminEdit.MTBankID.Visible (begin)
    <div id="r_MTBankID" @Html.Raw(crewBankAccountForAdminEdit.MTBankID.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_MTBankID" for="x_MTBankID" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_MTBankID">@Html.Raw(crewBankAccountForAdminEdit.MTBankID.Caption)@Html.Raw(crewBankAccountForAdminEdit.MTBankID.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.MTBankID.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_MTBankID"><span id="el_CrewBankAccountForAdmin_MTBankID">
    <select
        id="x_MTBankID"
        name="x_MTBankID"
        class="form-select ew-select@(crewBankAccountForAdminEdit.MTBankID.IsInvalidClass)"
        data-select2-id="fCrewBankAccountForAdminedit_x_MTBankID"
        data-table="CrewBankAccountForAdmin"
        data-field="x_MTBankID"
        data-value-separator="@(crewBankAccountForAdminEdit.MTBankID.DisplayValueSeparatorAttribute)"
        data-placeholder="@(HtmlEncode(crewBankAccountForAdminEdit.MTBankID.PlaceHolder))"
        @Html.Raw(crewBankAccountForAdminEdit.MTBankID.EditAttributes)>
        @(crewBankAccountForAdminEdit.MTBankID.SelectOptionListHtml("x_MTBankID"))
    </select>
    @Html.Raw(crewBankAccountForAdminEdit.MTBankID.GetCustomMessage)
    <div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.MTBankID.GetErrorMessage())</div>
@Html.Raw(crewBankAccountForAdminEdit.MTBankID.Lookup.GetParamTag(crewBankAccountForAdminEdit, "p_x_MTBankID"))
<script>
loadjs.ready("fCrewBankAccountForAdminedit", function() {
    var options = { name: "x_MTBankID", selectId: "fCrewBankAccountForAdminedit_x_MTBankID" },
        el = document.querySelector("select[data-select2-id='" + options.selectId + "']");
    if (!el)
        return;
    options.closeOnSelect = !options.multiple;
    options.dropdownParent = el.closest("#ew-modal-dialog, #ew-add-opt-dialog");
    if (fCrewBankAccountForAdminedit.lists.MTBankID?.lookupOptions.length) {
        options.data = { id: "x_MTBankID", form: "fCrewBankAccountForAdminedit" };
    } else {
        options.ajax = { id: "x_MTBankID", form: "fCrewBankAccountForAdminedit", limit: ew.LOOKUP_PAGE_SIZE };
    }
    options.minimumResultsForSearch = Infinity;
    options = Object.assign({}, ew.selectOptions, options, ew.vars.tables.CrewBankAccountForAdmin.fields.MTBankID.selectOptions);
    ew.createSelect(options);
});
</script>
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.MTBankID.Visible (end) *@
@if (crewBankAccountForAdminEdit.OtherBankName.Visible) { // crewBankAccountForAdminEdit.OtherBankName.Visible (begin)
    <div id="r_OtherBankName" @Html.Raw(crewBankAccountForAdminEdit.OtherBankName.RowAttributes)>
        <label id="elh_CrewBankAccountForAdmin_OtherBankName" for="x_OtherBankName" class="@(crewBankAccountForAdminEdit.LeftColumnClass)"><template id="tpc_CrewBankAccountForAdmin_OtherBankName">@Html.Raw(crewBankAccountForAdminEdit.OtherBankName.Caption)@Html.Raw(crewBankAccountForAdminEdit.OtherBankName.Required ? Language.Phrase("FieldRequiredIndicator") : "")</template></label>
        <div class="@(crewBankAccountForAdminEdit.RightColumnClass)"><div @Html.Raw(crewBankAccountForAdminEdit.OtherBankName.CellAttributes)>
<template id="tpx_CrewBankAccountForAdmin_OtherBankName"><span id="el_CrewBankAccountForAdmin_OtherBankName">
<input type="@(crewBankAccountForAdminEdit.OtherBankName.InputTextType)" name="x_OtherBankName" id="x_OtherBankName" data-table="CrewBankAccountForAdmin" data-field="x_OtherBankName" value="@Html.Raw(crewBankAccountForAdminEdit.OtherBankName.EditValue)" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(crewBankAccountForAdminEdit.OtherBankName.PlaceHolder))" data-format-pattern="@(crewBankAccountForAdminEdit.OtherBankName.FormatPattern)"@Html.Raw(crewBankAccountForAdminEdit.OtherBankName.EditAttributes) aria-describedby="x_OtherBankName_help">
@Html.Raw(crewBankAccountForAdminEdit.OtherBankName.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(crewBankAccountForAdminEdit.OtherBankName.GetErrorMessage())</div>
</span></template>
</div></div>
    </div>
} @* crewBankAccountForAdminEdit.OtherBankName.Visible (end) *@
</div><!-- /page* -->
    <input type="hidden" data-table="CrewBankAccountForAdmin" data-field="x_ID" data-hidden="1" name="x_ID" id="x_ID" value="@Html.Raw(HtmlEncode(crewBankAccountForAdminEdit.ID.CurrentValue))">
<div id="tpd_CrewBankAccountForAdminedit" class="ew-custom-template"></div>
<template id="tpm_CrewBankAccountForAdminedit">
<div id="ct_crewBankAccountForAdminEdit">    <ul class="nav nav-tabs">
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewPersonalDataForAdminEdit")">
                @(CurrentLanguage == "en-US" ? "PERSONAL DATA" : "DATA PERSONAL")
            </a>     
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewDocumentForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "DOCUMENTS" : "DOKUMEN")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewBankAccountForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "BANK ACCOUNT" : "REKENING BANK")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewFamilyForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "FAMILY" : "KELUARGA")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewFormalEducationForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "FORMAL EDUCATION" : "PENDIDIKAN FORMAL")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewMedicalCertificateForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "MEDICAL CERTIFICATE / TEST" : "SERTIFIKAT / TES MEDIS")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewCertificateForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "CERTIFICATES AND QUALIFICATIONS" : "SERTIFIKAT DAN KUALIFIKASI")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewExperienceForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "SEA EXPERIENCE" : "PENGALAMAN BERLAYAR")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewAppraisalForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "APPRAISALS" : "PENILAIAN")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewMedicalHistoryForAdminAdd")">
                @(CurrentLanguage == "en-US" ? "MEDICAL HISTORY" : "RIWAYAT MEDIS")
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@string.Concat(AppPath(), "CrewGeneralDataForAdminEdit")">
                @(CurrentLanguage == "en-US" ? "GENERAL" : "UMUM")
            </a>
        </li>
    </ul>
    <div class="mb-2 row">
        <div class="col-sm-6">
            <div id="r_MTBankID" class="mb-2 row">
                <label for="x_MTBankID" class="col-form-label">@Html.Raw(crewBankAccountForAdminEdit.MTBankID.Caption)<i class="fa-solid fa-asterisk ew-required"></i></label>
                <div><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_MTBankID"></slot></div>
            </div>
        </div>
        <div class="col-sm-6">
            <div id="r_OtherBankName" class="mb-2 row">
                <label for="x_OtherBankName" class="col-form-label">@Html.Raw(crewBankAccountForAdminEdit.OtherBankName.Caption)<i class="fa-solid fa-asterisk ew-required"></i></label>
                <div><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_OtherBankName"></slot></div>
            </div>
        </div>
    </div>
    <div class="mb-2 row">
        <div class="col-sm-6">
            <div id="r_AccountNumber" class="mb-2 row">
                <label for="x_AccountNumber" class="col-form-label">@Html.Raw(crewBankAccountForAdminEdit.AccountNumber.Caption)<i class="fa-solid fa-asterisk ew-required"></i></label>
                <div><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_AccountNumber"></slot></div>
            </div>
        </div>
        <div class="col-sm-6">
            <div id="r_Beneficiary" class="mb-2 row">
                <label for="x_Beneficiary" class="col-form-label">@Html.Raw(crewBankAccountForAdminEdit.Beneficiary.Caption)<i class="fa-solid fa-asterisk ew-required"></i></label>
                <div><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_Beneficiary"></slot></div>
            </div>
        </div>
    </div>
     <div class="mb-2 row">
        <div class="col-sm-6">
            <div id="r_MTCurrencyID" class="mb-2 row">
                <label for="x_MTCurrencyID" class="col-form-label">@Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.Caption)<i class="fa-solid fa-asterisk ew-required"></i></label>
                <div><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_MTCurrencyID"></slot></div>
                <div hidden><slot class="ew-slot" name="tpx_Currency"></slot></div>
            </div>
        </div>
        <div class="col-sm-6">
            <div id="r_MainAcc" class="mb-2 row">
                <label for="x_MainAcc" class="col-form-label">@Html.Raw(crewBankAccountForAdminEdit.MainAcc.Caption)<i class="fa-solid fa-asterisk ew-required"></i></label>
                <div><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_MainAcc"></slot></div>
            </div>
        </div>
    </div>
    <div class="mb-2 row">
        <div class="col-sm-6">
            <div id="r_Attachment" class="mb-2 row">
                <label for="x_Attachment" class="col-form-label">
                    @Html.Raw(crewBankAccountForAdminEdit.Attachment.Caption) @(CurrentLanguage == "en-US" ? "(jpg/jpeg/png/pdf max 2 MB)" : "(jpg/jpeg/png/pdf maks 2 MB)")<i class="fa-solid fa-asterisk ew-required"></i>
                </label>
                <div><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_Attachment"></slot></div>
            </div>
        </div>
        <div class="col-sm-6">
        </div>
    </div>
    <div class="row"><!-- buttons .row -->
        <div class="d-flex mb-2"><!-- buttons offset -->
            <button class="btn btn-primary ew-btn ms-auto primary-black-clr gray-bg" id="btn-update-entry">
                @(CurrentLanguage == "en-US" ? "Update Entry" : "Ubah")
            </button>
        </div><!-- /buttons offset -->
    </div>
    <div class="table-responsive">
        <table class="table table-bordered table-hover table-sm ew-table table-head-fixed" id="ForAdminTable" data-isset="true">
            <thead>
                <tr class="ew-table-header">
                    <th class="ew-list-option-header w-1" data-name="button"></th>
                    <th class="ew-list-option-header text-nowrap" data-name="sequence"><span></span></th>
                    <th data-name="MTBankID" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.MTBankID.Caption)
                    </th>
                    <th data-name="AccountNumber" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.AccountNumber.Caption)
                    </th>
                    <th data-name="Beneficiary" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.Beneficiary.Caption)
                    </th>              
                    <th data-name="MTCurrencyID" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.MTCurrencyID.Caption)
                    </th>
                    <th data-name="MainAcc" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.MainAcc.Caption)
                    </th>
                    <th data-name="Attachment" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.Attachment.Caption)
                    </th>         
                    <th data-name="CreatedByUserID" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.CreatedByUserID.Caption)
                    </th>
                    <th data-name="CreatedDateTime" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.CreatedDateTime.Caption)
                    </th>
                    <th data-name="LastUpdatedByUserID" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.LastUpdatedByUserID.Caption)
                    </th>
                    <th data-name="LastUpdatedDateTime" class="ew-table-header-cell theadnowrap">
                        @Html.Raw(crewBankAccountForAdminEdit.LastUpdatedDateTime.Caption)
                    </th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
    <div id="r_MTCrewID" class="mb-3 row">
        <label for="x_MTCrewID" class="col-sm-2 col-form-label">@Html.Raw(crewBankAccountForAdminEdit.MTCrewID.Caption)</label>
        <div class="col-sm-10"><slot class="ew-slot" name="tpx_CrewBankAccountForAdmin_MTCrewID"></slot></div>
    </div>
    <div id="proceed-modal-dialog" class="modal" aria-hidden="true" data-bs-backdrop="static" tabindex="-1" aria-labelledby="proceed-modal-dialog-title" aria-modal="true" role="dialog">
        <div class="modal-dialog modal-dialog-centered modal-md">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 id="proceed-modal-dialog-title" class="modal-title">
                        @(CurrentLanguage == "en-US" ? "Warning" : "Peringatan")
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p> 
                        @{
                            if (CurrentLanguage == "en-US")
                            {
                                <span>There are error(s) in this tab.</span><br/>
                                <span>If you choose to Proceed, any unsaved data will be lost.</span><br/>
                                <span>Do you want to Proceed?</span>
                            }
                            else
                            {
                                <span>Masih ada error di tab ini.</span><br/>
                                <span>Jika Anda memilih Lanjutkan, data yang belum disimpan akan hilang.</span><br/>
                                <span>Apakah Anda ingin melanjutkan?</span>
                            }
                        }
                    </p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-danger ew-btn" id="btn-proceed" type="button">
                        @(CurrentLanguage == "en-US" ? "Proceed" : "Lanjutkan")
                    </button>
                    <button class="btn btn-default ew-btn" id="btn-proceed-cancel" type="button" data-bs-dismiss="modal">
                        @(CurrentLanguage == "en-US" ? "Cancel" : "Batal")
                    </button>
                </div>
            </div>
        </div>
    </div>
    <div id="loading-modal-dialog" class="modal" aria-hidden="true" data-bs-backdrop="static" tabindex="-1" aria-modal="true" role="dialog">
        <div class="modal-dialog modal-dialog-centered modal-md">
            <div class="modal-content">
                <div class="modal-body">
                    <p class="text-center mb-0">
                        @(CurrentLanguage == "en-US" ? "Saving..." : "Menyimpan...")
                    </p>
                </div>
            </div>
        </div>
    </div>
    <div id="delete-modal-dialog" class="modal" aria-hidden="true" data-bs-backdrop="static" tabindex="-1" aria-labelledby="delete-modal-dialog-title" aria-modal="true" role="dialog">
        <div class="modal-dialog modal-dialog-centered modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 id="delete-modal-dialog-title" class="modal-title">
                        @(CurrentLanguage == "en-US" ? "Confirmation" : "Konfirmasi")
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>
                        @(CurrentLanguage == "en-US" ? "Are you sure you want to delete?" : "Anda yakin ingin menghapus?")
                    </p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-primary ew-btn" id="btn-delete" type="button">
                        @(CurrentLanguage == "en-US" ? "Yes" : "Iya")
                    </button>
                    <button class="btn btn-default ew-btn" id="btn-delete-cancel" type="button" data-bs-dismiss="modal">
                        @(CurrentLanguage == "en-US" ? "No" : "Tidak")
                    </button>
                </div>
            </div>
        </div>
    </div></div>
</template>
@{ crewBankAccountForAdminEdit.CloseRecordset(); }
@Html.Raw(crewBankAccountForAdminEdit.IsModal ? "<template class=\"ew-modal-buttons\">" : "<div class=\"row ew-buttons\">")<!-- buttons .row -->
    <div class="@(crewBankAccountForAdminEdit.OffsetColumnClass)"><!-- buttons offset -->
    <button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit" form="fCrewBankAccountForAdminedit">@Html.Raw(Language.Phrase("SaveBtn"))</button>
@if (IsJsonResponse()) {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-bs-dismiss="modal">@Html.Raw(Language.Phrase("CancelBtn"))</button>
} else {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" form="fCrewBankAccountForAdminedit" data-href="@AppPath(crewBankAccountForAdminEdit.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
}
    </div><!-- /buttons offset -->
@Html.Raw(crewBankAccountForAdminEdit.IsModal ? "</template>" : "</div>")<!-- /buttons .row -->
</form>
<script class="ew-apply-template">
loadjs.ready(ew.applyTemplateId, function() {
    var $ = jQuery;
    ew.templateData = { rows: @Html.Raw(ConvertToJson(crewBankAccountForAdminEdit.Rows)) };
    ew.applyTemplate("tpd_CrewBankAccountForAdminedit", "tpm_CrewBankAccountForAdminedit", "CrewBankAccountForAdminedit", "@(crewBankAccountForAdminEdit.Export)", "CrewBankAccountForAdmin", ew.templateData.rows[0], @(crewBankAccountForAdminEdit.IsModal ? "true" : "false"));
    loadjs.done("customtemplate");
});
</script>
</main>
@crewBankAccountForAdminEdit.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("CrewBankAccountForAdmin");
});
</script>
<script>
loadjs.ready("load", function() {
    // Startup script
    // Write your table-specific startup script here
    // document.write("page loaded");

    // Get the current URL
    const currentUrlObject = new URL(window.location.href);
    // get the current URL without Add or Edit for assigning active class in tabs
    const currentUrlWithoutAddEdit = currentUrlObject.href.replace(/Add|Edit/g, "");
    // Create a new URLSearchParams object based on the current URL's search parameters
    const urlQueryParams = new URLSearchParams(currentUrlObject.search);
    // Extract the value of the crewID query parameter
    const currentlyEditedCrewID = urlQueryParams.get('crewID');
    const crewBankAccountID = parseInt(currentUrlObject.pathname.split('/').pop()); 
    const selectedLanguage = ew.vars['languages']['languages'].filter(obj => obj.selected === true)[0];
    const parentUrl = ew.PATH_BASE;
    const tableName = "CrewBankAccountForAdmin";
    const pageType = "edit";
    const pageTypeCapital = "Edit";
    const formID = `f${tableName}${pageType}`;
    const updateEntryUrl = `${parentUrl}${tableName}${pageTypeCapital}/${crewBankAccountID}?crewID=${currentlyEditedCrewID}`;
    const addPageUrl = `${parentUrl}${tableName}Add?crewID=${currentlyEditedCrewID}`;
    let isChangeTabApproved = false;
    const adminAndEmployeeStatusApi = 'get-admin-and-employee-status';
    $.ajax({
        url: `${parentUrl}api/Registration/${adminAndEmployeeStatusApi}`,
        method: 'GET',
        data: {
            crewID: currentlyEditedCrewID
        },
        success: function(response) {
            if (response.success) {
                const isAdmin = response.data.IsAdmin;
                if (isAdmin) {
                    if (currentlyEditedCrewID === null) {
                        window.location.replace(`${ew.PATH_BASE}RegistrationEdit/-1`);
                    }
                    const originalCancelHref = $('#btn-cancel').attr('data-href');
                    const customCancelButton = $('<button/>', {
                        'class': 'btn btn-danger ew-btn ms-auto primary-red-bg primary-white-clr',
                        'id': 'btn-custom-cancel',
                        'type': 'button',
                        'text': `${ew.language.phrase("CancelBtn")}`,
                    }).click(function() {
                        if (window[formID].modified) {
                            ew.prompt(ew.language.phrase("ConfirmCancel"), result => {
                                if (result) {
                                    isChangeTabApproved = true;
                                    proceedToLink(originalCancelHref);
                                }
                              });
                        } else {
                            isChangeTabApproved = true;
                            proceedToLink(originalCancelHref);
                        }
                    });
                    $('#btn-cancel').after(customCancelButton);
                    $('#btn-cancel').remove();
                } else {
                    if (currentlyEditedCrewID === null) {
                        window.location.replace(`${ew.PATH_BASE}index`);
                    }
                    $('#btn-cancel').addClass('ms-auto primary-red-bg primary-white-clr');
                }
            } else {
                ew.showToast(response.errorMessage);
            }
        },
        error: function(xhr, textStatus, errorThrown) {
            ew.showToast(errorThrown);
        }
    });
    const entryButton = $('<button/>', {
        'class': 'btn btn-primary ew-btn ms-auto primary-black-clr gray-bg',
        'id': 'btn-entry',
        'type': 'button',
        'text': (selectedLanguage.id === 'en-US') ? `Update Entry` : `Ubah`
    }).click(function() {
        const validateUpdateEntryResult = validateUpdateEntry();
        if (validateUpdateEntryResult) {
            $('#loading-modal-dialog').modal('show');
            const targetUrlRelative = addPageUrl;
            updateEntry({ redirectUrlWhenSuccess: targetUrlRelative });
        } else {
            scrollToFirstInvalidInput();
        }
    });
    $('#btn-update-entry').after(entryButton);
    $('#btn-update-entry').remove();
    $('#btn-delete').click(function() {
        const crewBankAccountID = $(this).attr('crew-bank-account-id');
        $.ajax({
            url: `${parentUrl}api/delete/${tableName}/${crewBankAccountID}`,
            type: 'GET',
            success: function(res) {
                if (res && res.success) {
                    $('#delete-modal-dialog').modal('hide');
                    isChangeTabApproved = true;
                    proceedToLink(addPageUrl);
                } else {
                    $('#delete-modal-dialog').modal('hide');
                    ew.showToast(res.failureMessage);
                }
            },
            error: function(xhr, textStatus, errorThrown) {
                ew.showToast(errorThrown);
                $('#delete-modal-dialog').modal('hide');
            }
        });
    });

    // to auto clear error message when focus for file fields
    $('#x_Attachment').on('focus', function() {
        $(this).removeClass('is-invalid');
    });
    $('#ft_x_Attachment button').on("click", function () {
      window[formID].modified = true;
    });

    // Iterate over each anchor element inside the nav-tabs
    $(".nav-tabs .nav-item a").each(function() {
        const href = $(this).attr("href");
        const hrefWithoutAddEdit = href.replace(/Add|Edit/g, "");
        // Check if the href contains current page's url
        if (currentUrlWithoutAddEdit.includes(hrefWithoutAddEdit)) {
            // add class and attribute
            $(this).addClass("active");
            $(this).attr("aria-current", "page");
        }
        // Check if the href contains "Edit" or "Add"
        if (href.includes("Edit")) {
            // Add "/" and the currently edited crew ID to the href
            const newHref = href + "/" + currentlyEditedCrewID;
            // Set the updated href attribute value
            $(this).attr("href", newHref);
        } else if (href.includes("Add")) {
            // Add "?crewID=" and the currently edited crew ID to the href
            const newHref = href + "?crewID=" + currentlyEditedCrewID;
            // Set the updated href attribute value
            $(this).attr("href", newHref);
        }
    });

    // start code for auto capitalize first letter of each word
    $('#x_Beneficiary').on('input', function() {
        $(this).val($(this).val().replace(/(?:^|\s)\S/g, function(a) { return a.toUpperCase(); }));
    });
    // end code for auto capitalize first letter of each word

    // Target the ol element that has breadcrumb class
    const $breadCrumbOl = $('.breadcrumb').first();

    // Delete the ol element that has breadcrumb class
    $breadCrumbOl.remove();
    $('#btn-action').remove();
    $('.ew-buttons > .col-sm-10').removeClass('col-sm-10').addClass('d-flex mb-3');
    $('.ew-buttons > .offset-sm-2').removeClass('offset-sm-2');

    //  programatically change the value of MTCrewID input and then hide it
    $('#x_MTCrewID').val(currentlyEditedCrewID);
    $('div#r_MTCrewID').hide();
    function toggleOtherBankName(selectedMTBankIDText) {
        if (selectedMTBankIDText === `<div><span>Other/Lainnya</span></div>`) {
            $('#x_OtherBankName').prop('disabled', false);
        } else {
            $('#x_OtherBankName').prop('disabled', true);
            $('#x_OtherBankName').val(null); 
        }
    }
    $('#x_MTBankID').on('select2:select', function (e) {
        const selectedMTBankIDText = e.params.data.text;
        toggleOtherBankName(selectedMTBankIDText);
    });
    const selectedMTBankIDText = $('#x_MTBankID').select2('data').length > 0 ? $('#x_MTBankID').select2('data')[0].text : null;
    toggleOtherBankName(selectedMTBankIDText);

    // get list of crew documents via API
    const uploadFolderName = "uploads";
    const baseUrl = currentUrlObject.origin + ew.PATH_BASE;
    const apiEndpoint = 'crew-bank-account-for-admin';
    $.ajax({
      url: `${parentUrl}api/Registration/${apiEndpoint}`,
      method: 'GET',
      data: {
        crewID: currentlyEditedCrewID
      },
      success: function(response) {
        if (response.success) {
          // Parse the data in the response
          const dataArray = response.data;
          const tbody = $('#ForAdminTable tbody'); // select the tbody element
          if (dataArray.length > 0) {
              const tableName = 'CrewBankAccountForAdmin';
              for (let rowIndex = 0; rowIndex < dataArray.length; rowIndex++) {
                const row = dataArray[rowIndex];
                const rowID = row['ID'];
                delete row['ID']; // delete ID for easier looping for table columns
                const tr = $('<tr>'); // create a new tr element 

                // first td for dropdown
                const buttonsTd = $('<td>').addClass('ew-list-option-body w-1');
                const buttonDropdownDiv = $('<div>').addClass('btn-group btn-group-sm ew-btn-dropdown');
                const dropdownButton = $('<button>')
                    .addClass('btn-default btn dropdown-toggle')
                    .attr('type', 'button')
                    .attr('data-title', 'Options')
                    .attr('data-bs-toggle', 'dropdown')
                    .attr('data-bs-auto-close', 'true');
                const dropdownButtonIcon = $('<i>').addClass('fa-solid fa-bars ew-icon').attr('data-phrase', 'ButtonListOptions');
                const dropdownButtonIconSpan = $('<span>').addClass('visually-hidden').text('Options');
                const dropdownUl = $('<ul>').addClass('dropdown-menu ew-dropdown-menu ew-list-options');
                const firstDropdownLi = $('<li>');
                const editUrl = `${parentUrl}${tableName}Edit/${rowID}?crewID=${currentlyEditedCrewID}`;
                const editAnchor = $('<a>')
                    .addClass('ew-row-link ew-edit dropdown-item')
                    .attr('data-caption', 'Edit')
                    .attr('href', editUrl)
                    .on('click', function(event) {
                        event.preventDefault();
                        isChangeTabApproved = true;
                        proceedToLink(editUrl);
                    });
                const editAnchorIcon = $('<i>').addClass('fa-solid fa-pen ew-icon me-2').attr('data-phrase', 'EditLink');
                const editAnchorSpan = $('<span>').addClass('visually-hidden').text('Edit');
                const editAnchorText= 'Edit';
                const secondDropdownLi = $('<li>');
                const deleteAnchor = $('<a>')
                    .addClass('ew-row-link dropdown-item')
                    .attr('data-caption', 'Delete')
                    .on('click', function(event) {
                        event.preventDefault();
                        $('#btn-delete').attr('crew-bank-account-id', rowID);
                        $('#delete-modal-dialog').modal('show');
                    });
                const deleteAnchorIcon = $('<i>').addClass('fa-solid fa-trash ew-icon me-2').attr('data-phrase', 'DeleteLink');
                const deleteAnchorSpan = $('<span>').addClass('visually-hidden').text('Delete');
                const deleteAnchorText= 'Delete';
                deleteAnchorIcon.append(deleteAnchorSpan);
                deleteAnchor.append(deleteAnchorIcon);
                deleteAnchor.append(deleteAnchorText);
                secondDropdownLi.append(deleteAnchor);
                editAnchorIcon.append(editAnchorSpan);
                editAnchor.append(editAnchorIcon);
                editAnchor.append(editAnchorText);
                firstDropdownLi.append(editAnchor);
                dropdownUl.append(firstDropdownLi);
                dropdownUl.append(secondDropdownLi);
                dropdownButtonIcon.append(dropdownButtonIconSpan);
                dropdownButton.append(dropdownButtonIcon);
                buttonDropdownDiv.append(dropdownButton);
                buttonDropdownDiv.append(dropdownUl);
                buttonsTd.append(buttonDropdownDiv);
                tr.append(buttonsTd);

                // second td for sequence
                const sequenceTd = $('<td>').addClass('ew-list-option-body text-nowrap').attr('data-name', 'sequence');
                const sequenceNumberString = (rowIndex + 1).toString() + '.';
                const sequenceSpan = $('<span>').text(sequenceNumberString);
                sequenceTd.append(sequenceSpan);
                tr.append(sequenceTd);

                // remaining td for each column
                for (const columnName of Object.keys(row)) {
                  const dataTd = $('<td>');
                  const columnValue = row[columnName];
                  if (columnName !== "Attachment") {
                    const dataSpan = $('<span>').text(columnValue);
                    dataTd.append(dataSpan);
                  } else {
                    dataTd.addClass("text-center");
                    const individualCodeNumber = columnValue.split('-')[0];
                    const imageUrl = `${baseUrl}${uploadFolderName}/${individualCodeNumber}/${columnValue}`;
                    const anchorLink = $('<a>', {
                      href: imageUrl,
                      target: '_blank'
                    });
                    anchorLink.text("Attachment");
                    dataTd.append(anchorLink);
                  }
                  tr.append(dataTd);
                }
                tbody.append(tr);
              }
          } else {
              const columnsCount = $('#ForAdminTable th.ew-table-header-cell').length;
              for (let rowIndex = 0; rowIndex < 3; rowIndex++) {
                const tr = $('<tr>'); // create a new tr element 

                // 1 is for buttons dropdown(for edit) column and 1 for sequence number column
                const emptyTdWithColspan = $('<td>').html('&nbsp;').attr('colspan', 2); 
                tr.append(emptyTdWithColspan);
                for (let columnIndex = 0; columnIndex < columnsCount; columnIndex++) {
                  const emptyTd = $('<td>').html('&nbsp;');
                  tr.append(emptyTd);
                }
                tbody.append(tr);
              }
          }
        } else {
          // Handle the error
          ew.showToast(response.errorMessage);
        }
      },
      error: function(xhr, textStatus, errorThrown) {
        // Handle the error
        ew.showToast(errorThrown);
      }
    });

    // AUTO SAVE WHEN MOVING FROM THIS TAB
    $(window).on('beforeunload', function(event) {
        if (!isChangeTabApproved && window[formID].modified) {
            event.preventDefault();
            event.returnValue = '';
            return '';
        }
    });
    $("#btn-proceed").click(function() {
        isChangeTabApproved = true;
        const targetUrlRelative = $(this).attr('target-url-relative');
        $('#approve-modal-dialog').modal('hide');
        proceedToLink(targetUrlRelative);
    });
    $('ul.nav.nav-tabs li.nav-item a.nav-link').on('click', function(event) {
        event.preventDefault();
        const targetUrlRelative = $(this).attr('href');
        if (window[formID].modified) {
            const validateUpdateEntryResult = validateUpdateEntry();
            if (validateUpdateEntryResult) {
                $('#loading-modal-dialog').modal('show');
                updateEntry({ redirectUrlWhenSuccess: targetUrlRelative });
            } else {
                scrollToFirstInvalidInput();
                $('#btn-proceed').attr('target-url-relative', targetUrlRelative);
                $('#proceed-modal-dialog').modal('show');
            }
        } else {
            isChangeTabApproved = true;
            proceedToLink(targetUrlRelative);
        }
    });
    function proceedToLink(targetUrlRelative) {
        const baseUrl = new URL(window.location.href).origin;
        const targetUrl = new URL(baseUrl + targetUrlRelative);
        window.location.href = targetUrl.href;
    }
    function scrollToFirstInvalidInput() {
        const invalidInput = $("input.is-invalid:first");
        if (invalidInput.length > 0) { // Check if an invalid input is found
            const offset = invalidInput.offset().top; // Calculate the offset of the invalid input element from the top of the page
            const windowHeight = $(window).height(); // Calculate the height of the viewport
            const scrollPosition = offset - (windowHeight / 2); // Calculate the scroll position to center the invalid input element
            $("html, body").animate({ // Animate the scroll to the center position
                scrollTop: scrollPosition
            }, 1000); // Adjust the duration as needed (in milliseconds)
        }
    }
    function updateEntry({ redirectUrlWhenSuccess }) {
        isChangeTabApproved = true;
        let formData = $(`form#${formID}`).serialize();
        $('input[type="checkbox"]').each(function() {
            // Jika checkbox tidak dicentang dan tidak ada di hasil serialize(),
            // tambahkan nilai 0 ke hasil serialize()
            if (!this.checked && formData.indexOf(this.name) === -1) {
                formData += '&' + this.name + '=0';
            }
        });
        $.ajax({
            url: updateEntryUrl,
            type: 'POST',
            data: formData,
            success: function(res) {
                const $errorDiv = $(res).find('div.ew-error'); // res is a string containing html document
                $('#loading-modal-dialog').modal('hide');
                if ($errorDiv.length > 0) {
                    ew.showToast($errorDiv.text());
                } else {
                    proceedToLink(redirectUrlWhenSuccess);
                }
            },
            error: function(xhr, textStatus, errorThrown) {
                $('#loading-modal-dialog').modal('hide');
                isChangeTabApproved = false;
                ew.showToast(errorThrown);
            }
        });
    }
    function validateUpdateEntry() {
        let formErrorCount = 0;
        function validateRequiredField(fieldName) {
            const errorMessage = (selectedLanguage.id === 'en-US') ? "Please enter required field" : "Masukkan input yang dibutuhkan";
            const fieldNameWithPrefix = `x_${fieldName}`;
            const fieldCaption = $(`label[for=${fieldNameWithPrefix}]`).text();
            const fieldValue = $(`#${fieldNameWithPrefix}`).val();
            const errorMessageWithFieldName = `${errorMessage} - ${fieldCaption}`;
            if (fieldValue === '' || fieldValue === null) {
                window[formID].fields[fieldName].addError({ custom: errorMessageWithFieldName });
                window[formID].fields[fieldName].updateFeedback();
                window[formID].addError(fieldName, { custom: errorMessageWithFieldName });
                return false;
            } else {
                return true;
            }
        }
        function validateRequiredFileField(fieldName) {
            const errorMessage = (selectedLanguage.id === 'en-US') ? "Please enter required field" : "Masukkan input yang dibutuhkan";
            const fieldNameWithPrefixFn = `fn_x_${fieldName}`;
            const fieldNameWithPrefix = `x_${fieldName}`;
            const fieldCaption = $(`label[for=${fieldNameWithPrefix}]`).text();
            const fieldValue = $(`#${fieldNameWithPrefixFn}`).val();
            const errorMessageWithFieldName = `${errorMessage} - ${fieldCaption}`;
            if (fieldValue === '') {
                window[formID].fields[fieldName].addError({ custom: errorMessageWithFieldName });
                window[formID].fields[fieldName].updateFeedback();
                window[formID].addError(fieldName, { custom: errorMessageWithFieldName });
                $(`#${fieldNameWithPrefix}`).addClass('is-invalid');
                return false;
            } else {
                return true;
            }
        }
        function validateFieldWithRegex(fieldName, errorMessage, regex) {
            const fieldNameWithPrefix = `x_${fieldName}`;
            const fieldValue = $(`#${fieldNameWithPrefix}`).val();
            if (!regex.test(fieldValue)) {  
                window[formID].fields[fieldName].addError({ custom: errorMessage });
                window[formID].fields[fieldName].updateFeedback();
                window[formID].addError(fieldName, { custom: errorMessage });
                return false;
            } else {
                return true;
            }
        }
        const digitsOnlyRegex = /^\d+$/;
        const digitsOnlyRegexErrorMessage = (selectedLanguage.id === 'en-US') ? "Must contain digits only" : "Harus terdiri dari angka saja";
        const noDigitsRegex = /^[^0-9]+$/;
        const noDigitsRegexErrorMessage = (selectedLanguage.id === 'en-US') ? "Must not contain digits" : "Tidak boleh mengandung angka";
        const otherBankNameErrorMessage = (selectedLanguage.id === 'en-US') ? `Cannot be empty when selecting Other/Lainnya` : `Tidak boleh kosong ketika memilih Other/Lainnya`;
        const mtBankIDText = $('#x_MTBankID').select2('data').length > 0 ? $('#x_MTBankID').select2('data')[0].text : null;
        if (!validateRequiredField("MTBankID")) {
            formErrorCount += 1;
        } else if (mtBankIDText === `<div><span>Other/Lainnya</span></div>`) {
            const otherBankNameValue = $("#x_OtherBankName").val();
            if (otherBankNameValue === '') {
                window[formID].fields["OtherBankName"].addError({ custom: otherBankNameErrorMessage });
                window[formID].fields["OtherBankName"].updateFeedback();
                window[formID].addError("OtherBankName", { custom: otherBankNameErrorMessage }); 
                formErrorCount += 1;
            }
        }
        if (!validateRequiredField("AccountNumber")) {
            formErrorCount += 1;
        } else {
            formErrorCount += validateFieldWithRegex("AccountNumber",  digitsOnlyRegexErrorMessage, digitsOnlyRegex) ? 0 : 1;
        }
        if (!validateRequiredField("Beneficiary")) {
            formErrorCount += 1;
        } else {
            formErrorCount += validateFieldWithRegex("Beneficiary",  noDigitsRegexErrorMessage, noDigitsRegex) ? 0 : 1;
        }
        if (!validateRequiredField("MTCurrencyID")) {
            formErrorCount += 1;
        }
        if (!validateRequiredFileField("Attachment")) {
            formErrorCount += 1;
        }
        return formErrorCount === 0;
    }
});
</script>
