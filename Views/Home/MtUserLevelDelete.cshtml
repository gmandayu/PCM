@{
    // View
    View = this;

    // Check action result
    if (mtUserLevelDelete.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!mtUserLevelDelete.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
<script>
var currentTable = @Html.Raw(ConvertToJson(mtUserLevelDelete.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { MTUserLevel: currentTable } });
var currentPageID = ew.PAGE_ID = "delete";
var currentForm;
var fMTUserLeveldelete;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fMTUserLeveldelete")
        .setPageId("delete")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@mtUserLevelDelete.ShowPageHeader()
@mtUserLevelDelete.ShowMessages()
<form name="fMTUserLeveldelete" id="fMTUserLeveldelete" class="ew-form ew-delete-form" action="@AppPath(CurrentPageName())" method="post" autocomplete="on">
@if (CurrentPage.CheckToken) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="delete">
@foreach (string key in mtUserLevelDelete.RecordKeys) {
<input type="hidden" name="key_m[]" value="@key">
}
<div class="card ew-card ew-grid @(mtUserLevelDelete.TableGridClass)">
<div class="card-body ew-grid-middle-panel @(mtUserLevelDelete.TableContainerClass)" style="@(mtUserLevelDelete.TableContainerStyle)">
<table class="@(mtUserLevelDelete.TableClass)">
    <thead>
    <tr class="ew-table-header">
@if (mtUserLevelDelete.UserLevelID.Visible) { // mtUserLevelDelete.UserLevelID.Visible (begin)
        <th class="@Html.Raw(mtUserLevelDelete.UserLevelID.HeaderCellClass)"><span id="elh_MTUserLevel_UserLevelID" class="MTUserLevel_UserLevelID">@Html.Raw(mtUserLevelDelete.UserLevelID.Caption)</span></th>
} @* mtUserLevelDelete.UserLevelID.Visible (end) *@
@if (mtUserLevelDelete.UserLevelName.Visible) { // mtUserLevelDelete.UserLevelName.Visible (begin)
        <th class="@Html.Raw(mtUserLevelDelete.UserLevelName.HeaderCellClass)"><span id="elh_MTUserLevel_UserLevelName" class="MTUserLevel_UserLevelName">@Html.Raw(mtUserLevelDelete.UserLevelName.Caption)</span></th>
} @* mtUserLevelDelete.UserLevelName.Visible (end) *@
    </tr>
    </thead>
    <tbody>
@{ mtUserLevelDelete.RecordCount = 0; }
@while (mtUserLevelDelete.Recordset != null && await mtUserLevelDelete.Recordset.ReadAsync()) { // DN
    mtUserLevelDelete.RecordCount++;
    mtUserLevelDelete.RowCount++;

    // Set row properties
    mtUserLevelDelete.ResetAttributes();
    mtUserLevelDelete.RowType = RowType.View; // View

    // Get the field contents
    await mtUserLevelDelete.LoadRowValues(mtUserLevelDelete.Recordset);

    // Render row
    await mtUserLevelDelete.RenderRow();
    <tr @Html.Raw(mtUserLevelDelete.RowAttributes)>
@if (mtUserLevelDelete.UserLevelID.Visible) { // mtUserLevelDelete.UserLevelID.Visible (begin)
        <td @Html.Raw(mtUserLevelDelete.UserLevelID.CellAttributes)>
<span id="el@(mtUserLevelDelete.RowCount)_MTUserLevel_UserLevelID" class="el_MTUserLevel_UserLevelID">
<span @Html.Raw(mtUserLevelDelete.UserLevelID.ViewAttributes)>
@Html.Raw(mtUserLevelDelete.UserLevelID.GetViewValue())</span>
</span></td>
} @* mtUserLevelDelete.UserLevelID.Visible (end) *@
@if (mtUserLevelDelete.UserLevelName.Visible) { // mtUserLevelDelete.UserLevelName.Visible (begin)
        <td @Html.Raw(mtUserLevelDelete.UserLevelName.CellAttributes)>
<span id="el@(mtUserLevelDelete.RowCount)_MTUserLevel_UserLevelName" class="el_MTUserLevel_UserLevelName">
<span @Html.Raw(mtUserLevelDelete.UserLevelName.ViewAttributes)>
@Html.Raw(mtUserLevelDelete.UserLevelName.GetViewValue())</span>
</span></td>
} @* mtUserLevelDelete.UserLevelName.Visible (end) *@
    </tr>
}
@{ mtUserLevelDelete.CloseRecordset(); }
</tbody>
</table>
</div>
</div>
<div class="ew-buttons ew-desktop-buttons">
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("DeleteBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(mtUserLevelDelete.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
</div>
</form>
@mtUserLevelDelete.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
