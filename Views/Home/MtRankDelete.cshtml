@{
    // View
    View = this;

    // Check action result
    if (mtRankDelete.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!mtRankDelete.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
<script>
var currentTable = @Html.Raw(ConvertToJson(mtRankDelete.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { MTRank: currentTable } });
var currentPageID = ew.PAGE_ID = "delete";
var currentForm;
var fMTRankdelete;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fMTRankdelete")
        .setPageId("delete")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@mtRankDelete.ShowPageHeader()
@mtRankDelete.ShowMessages()
<form name="fMTRankdelete" id="fMTRankdelete" class="ew-form ew-delete-form" action="@AppPath(CurrentPageName())" method="post" autocomplete="on">
@if (CurrentPage.CheckToken) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="delete">
@foreach (string key in mtRankDelete.RecordKeys) {
<input type="hidden" name="key_m[]" value="@key">
}
<div class="card ew-card ew-grid @(mtRankDelete.TableGridClass)">
<div class="card-body ew-grid-middle-panel @(mtRankDelete.TableContainerClass)" style="@(mtRankDelete.TableContainerStyle)">
<table class="@(mtRankDelete.TableClass)">
    <thead>
    <tr class="ew-table-header">
@if (mtRankDelete._Name.Visible) { // mtRankDelete._Name.Visible (begin)
        <th class="@Html.Raw(mtRankDelete._Name.HeaderCellClass)"><span id="elh_MTRank__Name" class="MTRank__Name">@Html.Raw(mtRankDelete._Name.Caption)</span></th>
} @* mtRankDelete._Name.Visible (end) *@
@if (mtRankDelete.CreatedByUserID.Visible) { // mtRankDelete.CreatedByUserID.Visible (begin)
        <th class="@Html.Raw(mtRankDelete.CreatedByUserID.HeaderCellClass)"><span id="elh_MTRank_CreatedByUserID" class="MTRank_CreatedByUserID">@Html.Raw(mtRankDelete.CreatedByUserID.Caption)</span></th>
} @* mtRankDelete.CreatedByUserID.Visible (end) *@
@if (mtRankDelete.CreatedDateTime.Visible) { // mtRankDelete.CreatedDateTime.Visible (begin)
        <th class="@Html.Raw(mtRankDelete.CreatedDateTime.HeaderCellClass)"><span id="elh_MTRank_CreatedDateTime" class="MTRank_CreatedDateTime">@Html.Raw(mtRankDelete.CreatedDateTime.Caption)</span></th>
} @* mtRankDelete.CreatedDateTime.Visible (end) *@
@if (mtRankDelete.LastUpdatedByUserID.Visible) { // mtRankDelete.LastUpdatedByUserID.Visible (begin)
        <th class="@Html.Raw(mtRankDelete.LastUpdatedByUserID.HeaderCellClass)"><span id="elh_MTRank_LastUpdatedByUserID" class="MTRank_LastUpdatedByUserID">@Html.Raw(mtRankDelete.LastUpdatedByUserID.Caption)</span></th>
} @* mtRankDelete.LastUpdatedByUserID.Visible (end) *@
@if (mtRankDelete.LastUpdatedDateTime.Visible) { // mtRankDelete.LastUpdatedDateTime.Visible (begin)
        <th class="@Html.Raw(mtRankDelete.LastUpdatedDateTime.HeaderCellClass)"><span id="elh_MTRank_LastUpdatedDateTime" class="MTRank_LastUpdatedDateTime">@Html.Raw(mtRankDelete.LastUpdatedDateTime.Caption)</span></th>
} @* mtRankDelete.LastUpdatedDateTime.Visible (end) *@
    </tr>
    </thead>
    <tbody>
@{ mtRankDelete.RecordCount = 0; }
@while (mtRankDelete.Recordset != null && await mtRankDelete.Recordset.ReadAsync()) { // DN
    mtRankDelete.RecordCount++;
    mtRankDelete.RowCount++;

    // Set row properties
    mtRankDelete.ResetAttributes();
    mtRankDelete.RowType = RowType.View; // View

    // Get the field contents
    await mtRankDelete.LoadRowValues(mtRankDelete.Recordset);

    // Render row
    await mtRankDelete.RenderRow();
    <tr @Html.Raw(mtRankDelete.RowAttributes)>
@if (mtRankDelete._Name.Visible) { // mtRankDelete._Name.Visible (begin)
        <td @Html.Raw(mtRankDelete._Name.CellAttributes)>
<span id="el@(mtRankDelete.RowCount)_MTRank__Name" class="el_MTRank__Name">
<span @Html.Raw(mtRankDelete._Name.ViewAttributes)>
@Html.Raw(mtRankDelete._Name.GetViewValue())</span>
</span></td>
} @* mtRankDelete._Name.Visible (end) *@
@if (mtRankDelete.CreatedByUserID.Visible) { // mtRankDelete.CreatedByUserID.Visible (begin)
        <td @Html.Raw(mtRankDelete.CreatedByUserID.CellAttributes)>
<span id="el@(mtRankDelete.RowCount)_MTRank_CreatedByUserID" class="el_MTRank_CreatedByUserID">
<span @Html.Raw(mtRankDelete.CreatedByUserID.ViewAttributes)>
@Html.Raw(mtRankDelete.CreatedByUserID.GetViewValue())</span>
</span></td>
} @* mtRankDelete.CreatedByUserID.Visible (end) *@
@if (mtRankDelete.CreatedDateTime.Visible) { // mtRankDelete.CreatedDateTime.Visible (begin)
        <td @Html.Raw(mtRankDelete.CreatedDateTime.CellAttributes)>
<span id="el@(mtRankDelete.RowCount)_MTRank_CreatedDateTime" class="el_MTRank_CreatedDateTime">
<span @Html.Raw(mtRankDelete.CreatedDateTime.ViewAttributes)>
@Html.Raw(mtRankDelete.CreatedDateTime.GetViewValue())</span>
</span></td>
} @* mtRankDelete.CreatedDateTime.Visible (end) *@
@if (mtRankDelete.LastUpdatedByUserID.Visible) { // mtRankDelete.LastUpdatedByUserID.Visible (begin)
        <td @Html.Raw(mtRankDelete.LastUpdatedByUserID.CellAttributes)>
<span id="el@(mtRankDelete.RowCount)_MTRank_LastUpdatedByUserID" class="el_MTRank_LastUpdatedByUserID">
<span @Html.Raw(mtRankDelete.LastUpdatedByUserID.ViewAttributes)>
@Html.Raw(mtRankDelete.LastUpdatedByUserID.GetViewValue())</span>
</span></td>
} @* mtRankDelete.LastUpdatedByUserID.Visible (end) *@
@if (mtRankDelete.LastUpdatedDateTime.Visible) { // mtRankDelete.LastUpdatedDateTime.Visible (begin)
        <td @Html.Raw(mtRankDelete.LastUpdatedDateTime.CellAttributes)>
<span id="el@(mtRankDelete.RowCount)_MTRank_LastUpdatedDateTime" class="el_MTRank_LastUpdatedDateTime">
<span @Html.Raw(mtRankDelete.LastUpdatedDateTime.ViewAttributes)>
@Html.Raw(mtRankDelete.LastUpdatedDateTime.GetViewValue())</span>
</span></td>
} @* mtRankDelete.LastUpdatedDateTime.Visible (end) *@
    </tr>
}
@{ mtRankDelete.CloseRecordset(); }
</tbody>
</table>
</div>
</div>
<div class="ew-buttons ew-desktop-buttons">
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("DeleteBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(mtRankDelete.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
</div>
</form>
@mtRankDelete.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
