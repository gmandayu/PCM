@{
    // View
    View = this;

    // Check action result
    if (mtReligionView.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!mtReligionView.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
@if (!mtReligionView.IsExport()) {
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
}
@if (!mtReligionView.IsExport()) {
<div class="btn-toolbar ew-toolbar">
    @mtReligionView.ExportOptions.RenderBody()
    @mtReligionView.OtherOptions.RenderBody()
</div>
}
@mtReligionView.ShowPageHeader()
@mtReligionView.ShowMessages()
<main class="view">
<form name="fMTReligionview" id="fMTReligionview" class="ew-form ew-view-form overlay-wrapper" action="@AppPath(CurrentPageName())" method="post" autocomplete="on">
@if (!mtReligionView.IsExport()) {
<script>
var currentTable = @Html.Raw(ConvertToJson(mtReligionView.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { MTReligion: currentTable } });
var currentPageID = ew.PAGE_ID = "view";
var currentForm;
var fMTReligionview;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fMTReligionview")
        .setPageId("view")
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
}
@if (CurrentPage.CheckToken) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="modal" value="@ConvertToInt(mtReligionView.IsModal)">
<table class="@(mtReligionView.TableClass)">
@if (mtReligionView._Name.Visible) { // mtReligionView._Name.Visible (begin)
    <tr id="r__Name" @Html.Raw(mtReligionView._Name.RowAttributes)>
        <td class="@(mtReligionView.TableLeftColumnClass)"><span id="elh_MTReligion__Name">@Html.Raw(mtReligionView._Name.Caption)</span></td>
        <td data-name="_Name" @Html.Raw(mtReligionView._Name.CellAttributes)>
<span id="el_MTReligion__Name">
<span @Html.Raw(mtReligionView._Name.ViewAttributes)>
@Html.Raw(mtReligionView._Name.GetViewValue())</span>
</span></td>
    </tr>
} @* mtReligionView._Name.Visible (end) *@
@if (mtReligionView.CreatedByUserID.Visible) { // mtReligionView.CreatedByUserID.Visible (begin)
    <tr id="r_CreatedByUserID" @Html.Raw(mtReligionView.CreatedByUserID.RowAttributes)>
        <td class="@(mtReligionView.TableLeftColumnClass)"><span id="elh_MTReligion_CreatedByUserID">@Html.Raw(mtReligionView.CreatedByUserID.Caption)</span></td>
        <td data-name="CreatedByUserID" @Html.Raw(mtReligionView.CreatedByUserID.CellAttributes)>
<span id="el_MTReligion_CreatedByUserID">
<span @Html.Raw(mtReligionView.CreatedByUserID.ViewAttributes)>
@Html.Raw(mtReligionView.CreatedByUserID.GetViewValue())</span>
</span></td>
    </tr>
} @* mtReligionView.CreatedByUserID.Visible (end) *@
@if (mtReligionView.CreatedDateTime.Visible) { // mtReligionView.CreatedDateTime.Visible (begin)
    <tr id="r_CreatedDateTime" @Html.Raw(mtReligionView.CreatedDateTime.RowAttributes)>
        <td class="@(mtReligionView.TableLeftColumnClass)"><span id="elh_MTReligion_CreatedDateTime">@Html.Raw(mtReligionView.CreatedDateTime.Caption)</span></td>
        <td data-name="CreatedDateTime" @Html.Raw(mtReligionView.CreatedDateTime.CellAttributes)>
<span id="el_MTReligion_CreatedDateTime">
<span @Html.Raw(mtReligionView.CreatedDateTime.ViewAttributes)>
@Html.Raw(mtReligionView.CreatedDateTime.GetViewValue())</span>
</span></td>
    </tr>
} @* mtReligionView.CreatedDateTime.Visible (end) *@
@if (mtReligionView.LastUpdatedByUserID.Visible) { // mtReligionView.LastUpdatedByUserID.Visible (begin)
    <tr id="r_LastUpdatedByUserID" @Html.Raw(mtReligionView.LastUpdatedByUserID.RowAttributes)>
        <td class="@(mtReligionView.TableLeftColumnClass)"><span id="elh_MTReligion_LastUpdatedByUserID">@Html.Raw(mtReligionView.LastUpdatedByUserID.Caption)</span></td>
        <td data-name="LastUpdatedByUserID" @Html.Raw(mtReligionView.LastUpdatedByUserID.CellAttributes)>
<span id="el_MTReligion_LastUpdatedByUserID">
<span @Html.Raw(mtReligionView.LastUpdatedByUserID.ViewAttributes)>
@Html.Raw(mtReligionView.LastUpdatedByUserID.GetViewValue())</span>
</span></td>
    </tr>
} @* mtReligionView.LastUpdatedByUserID.Visible (end) *@
@if (mtReligionView.LastUpdatedDateTime.Visible) { // mtReligionView.LastUpdatedDateTime.Visible (begin)
    <tr id="r_LastUpdatedDateTime" @Html.Raw(mtReligionView.LastUpdatedDateTime.RowAttributes)>
        <td class="@(mtReligionView.TableLeftColumnClass)"><span id="elh_MTReligion_LastUpdatedDateTime">@Html.Raw(mtReligionView.LastUpdatedDateTime.Caption)</span></td>
        <td data-name="LastUpdatedDateTime" @Html.Raw(mtReligionView.LastUpdatedDateTime.CellAttributes)>
<span id="el_MTReligion_LastUpdatedDateTime">
<span @Html.Raw(mtReligionView.LastUpdatedDateTime.ViewAttributes)>
@Html.Raw(mtReligionView.LastUpdatedDateTime.GetViewValue())</span>
</span></td>
    </tr>
} @* mtReligionView.LastUpdatedDateTime.Visible (end) *@
</table>
@{ mtReligionView.CloseRecordset(); }
</form>
</main>
@mtReligionView.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
@if (!mtReligionView.IsExport()) {
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
}
