@{
    // View
    View = this;

    // Check action result
    if (mtCertificateAdd.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!mtCertificateAdd.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
<script>
var currentTable = @Html.Raw(ConvertToJson(mtCertificateAdd.ToClientVar()));
ew.deepAssign(ew.vars, { tables: { MTCertificate: currentTable } });
var currentPageID = ew.PAGE_ID = "add";
var currentForm;
var fMTCertificateadd;
loadjs.ready(["wrapper", "head"], function () {
    let $ = jQuery;
    let fields = currentTable.fields;

    // Form object
    let form = new ew.FormBuilder()
        .setId("fMTCertificateadd")
        .setPageId("add")

        // Add fields
        .setFields([

            ["CategoryName", [fields.CategoryName.visible && fields.CategoryName.required ? ew.Validators.required(fields.CategoryName.caption) : null], fields.CategoryName.isInvalid],
            ["_Name", [fields._Name.visible && fields._Name.required ? ew.Validators.required(fields._Name.caption) : null], fields._Name.isInvalid]
        ])

        // Form_CustomValidate
        .setCustomValidate(
            function(fobj) { // DO NOT CHANGE THIS LINE!
                    // Your custom validation code here, return false if invalid.
                    return true;
                }
        )

        // Use JavaScript validation or not
        .setValidateRequired(ew.CLIENT_VALIDATE)

        // Dynamic selection lists
        .setLists({
        })
        .build();
    window[form.id] = form;
    currentForm = form;
    loadjs.done(form.id);
});
</script>
<script>
loadjs.ready("head", function() {
    // Write your client script here, no need to add script tags.
});
</script>
@mtCertificateAdd.ShowPageHeader()
@mtCertificateAdd.ShowMessages()
<form name="fMTCertificateadd" id="fMTCertificateadd" class="@(mtCertificateAdd.FormClassName)" action="@AppPath(CurrentPageName())" method="post" autocomplete="on">
@if (CurrentPage.CheckToken) {
    <input type="hidden" name="@Config.TokenNameKey" value="@Config.TokenName"><!-- CSRF token name -->
    @Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="insert">
<input type="hidden" name="modal" value="@ConvertToInt(mtCertificateAdd.IsModal)">
@if (IsJsonResponse()) {
<input type="hidden" name="json" value="1">
}
<input type="hidden" name="@(mtCertificateAdd.OldKeyName)" value="@Html.Raw(mtCertificateAdd.OldKey)">
<div class="ew-add-div"><!-- page* -->
@if (mtCertificateAdd.CategoryName.Visible) { // mtCertificateAdd.CategoryName.Visible (begin)
    <div id="r_CategoryName" @Html.Raw(mtCertificateAdd.CategoryName.RowAttributes)>
        <label id="elh_MTCertificate_CategoryName" for="x_CategoryName" class="@(mtCertificateAdd.LeftColumnClass)">@Html.Raw(mtCertificateAdd.CategoryName.Caption)@Html.Raw(mtCertificateAdd.CategoryName.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(mtCertificateAdd.RightColumnClass)"><div @Html.Raw(mtCertificateAdd.CategoryName.CellAttributes)>
<span id="el_MTCertificate_CategoryName">
<input type="@(mtCertificateAdd.CategoryName.InputTextType)" name="x_CategoryName" id="x_CategoryName" data-table="MTCertificate" data-field="x_CategoryName" value="@Html.Raw(mtCertificateAdd.CategoryName.EditValue)" size="30" maxlength="150" placeholder="@Html.Raw(HtmlEncode(mtCertificateAdd.CategoryName.PlaceHolder))" data-format-pattern="@(mtCertificateAdd.CategoryName.FormatPattern)"@Html.Raw(mtCertificateAdd.CategoryName.EditAttributes) aria-describedby="x_CategoryName_help">
@Html.Raw(mtCertificateAdd.CategoryName.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(mtCertificateAdd.CategoryName.GetErrorMessage())</div>
</span>
</div></div>
    </div>
} @* mtCertificateAdd.CategoryName.Visible (end) *@
@if (mtCertificateAdd._Name.Visible) { // mtCertificateAdd._Name.Visible (begin)
    <div id="r__Name" @Html.Raw(mtCertificateAdd._Name.RowAttributes)>
        <label id="elh_MTCertificate__Name" for="x__Name" class="@(mtCertificateAdd.LeftColumnClass)">@Html.Raw(mtCertificateAdd._Name.Caption)@Html.Raw(mtCertificateAdd._Name.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
        <div class="@(mtCertificateAdd.RightColumnClass)"><div @Html.Raw(mtCertificateAdd._Name.CellAttributes)>
<span id="el_MTCertificate__Name">
<input type="@(mtCertificateAdd._Name.InputTextType)" name="x__Name" id="x__Name" data-table="MTCertificate" data-field="x__Name" value="@Html.Raw(mtCertificateAdd._Name.EditValue)" size="30" maxlength="150" placeholder="@Html.Raw(HtmlEncode(mtCertificateAdd._Name.PlaceHolder))" data-format-pattern="@(mtCertificateAdd._Name.FormatPattern)"@Html.Raw(mtCertificateAdd._Name.EditAttributes) aria-describedby="x__Name_help">
@Html.Raw(mtCertificateAdd._Name.GetCustomMessage)
<div class="invalid-feedback">@Html.Raw(mtCertificateAdd._Name.GetErrorMessage())</div>
</span>
</div></div>
    </div>
} @* mtCertificateAdd._Name.Visible (end) *@
</div><!-- /page* -->
@{ mtCertificateAdd.CloseRecordset(); }
@Html.Raw(mtCertificateAdd.IsModal ? "<template class=\"ew-modal-buttons\">" : "<div class=\"row ew-buttons\">")<!-- buttons .row -->
    <div class="@(mtCertificateAdd.OffsetColumnClass)"><!-- buttons offset -->
    <button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit" form="fMTCertificateadd">@Html.Raw(Language.Phrase("AddBtn"))</button>
@if (IsJsonResponse()) {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-bs-dismiss="modal">@Html.Raw(Language.Phrase("CancelBtn"))</button>
} else {
    <button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" form="fMTCertificateadd" data-href="@AppPath(mtCertificateAdd.ReturnUrl)">@Html.Raw(Language.Phrase("CancelBtn"))</button>
}
    </div><!-- /buttons offset -->
@Html.Raw(mtCertificateAdd.IsModal ? "</template>" : "</div>")<!-- /buttons .row -->
</form>
@mtCertificateAdd.ShowPageFooter()
@Html.Raw(GetDebugMessage())
@ElapsedTime()
<script>
// Field event handlers
loadjs.ready("head", function() {
    ew.addEventHandlers("MTCertificate");
});
</script>
<script>
loadjs.ready("load", function() {
    // Write your table-specific startup script here
    // document.write("page loaded");
});
</script>
