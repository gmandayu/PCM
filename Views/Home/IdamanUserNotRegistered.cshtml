@{
    // View
    View = this;

    // Check action result
    if (idamanUserNotRegistered.ActionResult is EmptyResult) { // Terminate page
        return;
    }

    // Layout
    if (!idamanUserNotRegistered.UseLayout) // UseLayout = false // DN
        Layout = "";
    else
        Layout = "_Layout";

    // Local variables (Temp data)
    #pragma warning disable 168
    string? sqlWrk, filterWrk, whereWrk, curVal, dispVal;
    List<Dictionary<string, object>>? rswrk;
    string[]? arWrk;
    List<object?>? listWrk;
    object? guidWrk;
    Func<string>? lookupFilter;
    #pragma warning restore 168
}
@idamanUserNotRegistered.ShowMessages()
<h5>
    @(CurrentLanguage == "en-US" ? "Your email is not yet registered in Venus application, please contact the administrator." : "Email Anda belum terdaftar di aplikasi Venus, silahkan hubungi admin.")
</h5>
@Html.Raw(GetDebugMessage())
